# Shopkeeper Agent Rebuild Design System

## Product context

Shopkeeper Agent 是面向电商数据分析人员的自然语言问数工作台。用户最关心的
不是“AI 很聪明”，而是问题是否被正确理解、正在执行到哪一步、查询结果是否
可信。界面因此优先表达任务、状态和数据，不做营销型首页。

## Aesthetic direction

- **方向：** 工业编辑式数据工作台。
- **气质：** 像一本被认真维护的电商经营账簿，温暖但不怀旧，克制但不冰冷。
- **继承：** 保留原项目羊皮纸、墨色、苔绿、黄铜的辨识度。
- **避免：** 紫色渐变、装饰性卡片网格、所有元素使用相同大圆角、无意义阴影。

## Information hierarchy

1. 用户当前问题和最终数据结果。
2. Agent 当前运行状态、错误恢复动作。
3. 完整 LangGraph 执行细节、服务地址等辅助信息。

## Typography

- **中文 UI：** `Noto Sans SC`，正文 16px，行高 1.65。
- **标题：** `LXGW WenKai Screen`，只用于品牌标题和空状态标题。
- **数字与技术信息：** `JetBrains Mono`，启用 tabular numbers。
- **层级：** 12 / 14 / 16 / 20 / 28 / 48px。
- 正文不低于 16px；辅助标签允许 12px，但必须保持足够对比度。

## Color tokens

| Token | Value | Usage |
|---|---:|---|
| canvas | `#F4EFE5` | 页面背景 |
| surface | `#FCF9F2` | 主工作表面 |
| surface-strong | `#EAE1D2` | 导航、表头 |
| ink | `#1E201C` | 主文字 |
| muted | `#666C63` | 次级文字 |
| moss | `#2F6B4F` | 主操作、成功 |
| brass | `#A9742E` | 运行中、强调 |
| tomato | `#C94F3D` | 错误、停止 |
| info | `#376A82` | 信息状态 |

颜色不是装饰：苔绿代表可继续或成功，黄铜代表处理中，番茄红代表需要关注。
所有状态同时使用文字和图标，不能只依赖颜色。

## Spacing and shape

- 基础单位：4px。
- 间距序列：4 / 8 / 12 / 16 / 24 / 32 / 48 / 64px。
- 圆角：小控件 4px，面板 8px，浮层 12px，圆形只用于头像和状态点。
- 阴影只表达层级；普通分区优先使用留白和细边线。

## Layout

- `< 768px`：顶部工具栏、抽屉导航、纵向会话、固定底部输入区。
- `768–1199px`：240px 导航 + 主工作区。
- `>= 1200px`：280px 导航 + 最大 1040px 会话工作区。
- 结果表格允许自身横向滚动，页面主体禁止意外横向溢出。

## Motion

- 微交互 140ms，面板切换 220ms，使用 ease-out。
- 只对运行状态、展开折叠和焦点转移使用动画。
- `prefers-reduced-motion` 下关闭非必要动画和平滑滚动。

## Interaction states

界面必须覆盖初始、连接中、运行中、成功、空结果、可重试错误、不可重试错误、
用户停止和后端未就绪。错误信息显示公开文案与 request ID，不显示服务器内部异常。

## Accessibility

- 正文颜色对比度至少 4.5:1。
- 交互目标至少 44×44px。
- 状态变化通过 `aria-live` 宣告。
- 所有图标按钮都有可访问名称和可见焦点。
- Enter 提交，Shift+Enter 换行，Escape 停止当前查询。

## Not in this stage

- 自动图表推断：后端尚未返回稳定的维度/指标 Schema。
- 登录、权限和多租户：当前后端没有对应身份协议。
- 跨设备会话历史与深色主题：先保证核心问数链路完整、清晰、可测试。
